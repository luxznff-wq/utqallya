package com.utqallya.backend.entity.enums;

/**
 * Roles soportados por Utqallya. Solo existen dos tipos de usuario final
 * (pasajero y conductor) más el rol administrativo interno.
 */
public enum RoleName {
    PASSENGER,
    DRIVER,
    ADMIN
}
